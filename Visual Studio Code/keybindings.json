[
    // Create & name new file in folder, instead of opening a new untitled editor
    { "key": "ctrl+n",                "command": "explorer.newFile" },

    // Do this manually instead of using explorer.autoReveal
    { "key": "ctrl+q",                "command": "workbench.files.action.showActiveFileInExplorer" }
]
